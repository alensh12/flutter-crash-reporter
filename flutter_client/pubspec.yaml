name: tizen_crash_reporter
description: Sketch Flutter-Tizen client for the flutter-crash-reporter backend.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  shared_preferences: ^2.0.5
  uuid: ^3.0.6
